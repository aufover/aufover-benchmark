Separate args-prefix@divine is used, because `--symbolic` mode
and `--leakcheck` combined together. Divine in symbolic mode creates
a memory leak whenever `__VERIFIER_nondet*` functions are used.

See [Issue #10](https://divine.fi.muni.cz/trac/ticket/10)
